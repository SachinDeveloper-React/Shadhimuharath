import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const SetupPreferencesScreen = (props: Props) => {
  return (
    <View>
      <Text>SetupPreferencesScreen</Text>
    </View>
  );
};

export default SetupPreferencesScreen;

const styles = StyleSheet.create({});
